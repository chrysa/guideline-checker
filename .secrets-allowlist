# Paths and values exempt from the secret-assignment scanner
# (guidelines/languages/_common.yml :: secrets-via-env).
# Test fixtures legitimately contain fake secrets; they stay scanned for every
# other rule, but are not flagged by the entropy-based secret detector.
paths:
  - tests/**
values:
  - super-secret-should-not-leak
